# 告别写脚本！JSON 驱动的浏览器表单自动化工具 — web-auto-form

> 用 JSON 描述你的表单工作流。零代码、确定性执行、AI Agent 原生集成。

---

## 你还在为填表写代码吗？

想象这些场景：

- 你要在招聘网站上投 50 份简历，每份填一样的个人信息，只是岗位不同
- 你需要每天帮团队在内部系统里批量录入数据
- 你想让 Claude 或 ChatGPT 帮你自动填表，但它不会操作浏览器

传统方案是什么？打开 Playwright 文档，写几十行 Python 代码，处理选择器、等待、重试、异常……一个简单的表单填写任务，代码写了一百行。

**web-auto-form** 换了一种思路：**你只需要一个 JSON 文件，描述"做什么"，引擎自动处理"怎么做"。**

---

## web-auto-form 是什么？

web-auto-form 是一个基于 **Playwright** 的 JSON 配置驱动浏览器自动化引擎，专注做一件事：**可靠地、安全地填写网页表单**。

它的核心设计理念：

| 传统方案 | web-auto-form |
|---------|---------------|
| 写代码控制浏览器 | 写 JSON 描述工作流 |
| 学习 Playwright/Selenium API | 了解 13 种动作类型即可 |
| 手动处理重试和异常 | 引擎内置重试策略和诊断 |
| 自己处理隐私数据 | 内置 PII 自动脱敏 |
| AI Agent 需要额外封装 | 原生 function-calling 集成 |

---

## 一个例子感受一下

下面这个 JSON 文件完成了一个完整的职位申请流程：填写个人信息 → 条件判断工作经验 → 上传简历 → 提交 → 验证结果。

```json
{
  "url": "https://example.com/apply",
  "consent_statement": "自动提交职位申请。",
  "data": {
    "user": { "name": "张伟", "email": "zhangwei@example.com" },
    "resume_path": "file:///documents/resume.pdf"
  },
  "steps": [
    { "action": "fill", "selector": "#name",  "value": "{{user.name}}" },
    { "action": "fill", "selector": "#email", "value": "{{user.email}}" },
    { "action": "if", "condition": { "selector": "#has_exp", "state": "checked" },
      "then": [
        { "action": "fill", "selector": "#years", "value": "5" }
      ],
      "else": [
        { "action": "check", "selector": "#fresh", "value": "true" }
      ]
    },
    { "action": "upload", "selector": "input[type='file']", "value": "{{resume_path}}" },
    { "action": "click", "selector": "button[type='submit']",
      "selector_fallbacks": ["[data-testid='submit-btn']"] },
    { "action": "assert", "selector": ".success", "state": "visible",
      "on_fail": "retry", "max_retries": 3 }
  ]
}
```

运行只需一行命令：

```bash
web_auto_form run application.json
```

三秒后，浏览器自动完成所有操作，返回结构化结果。

---

## 核心特性一览

### 13 种动作类型，覆盖所有表单场景

| 类别 | 动作 | 说明 |
|------|------|------|
| **交互** | `navigate` `fill` `click` `select` `check` `upload` `press_key` `scroll` `handle_dialog` | 导航、填写、点击、下拉、复选框、文件上传、按键、滚动、弹窗处理 |
| **控制** | `wait` `if` `assert` | 等待元素/页面/延时/JS 表达式、条件分支、状态断言 |
| **数据** | `extract` | 运行时提取页面数据 |

### 智能选择器系统

选择器自动识别类型，无需显式声明：

- `#email` → ID 选择器
- `//div[@class='form']` → XPath
- `[name='email']` → name 属性
- `[data-testid='submit']` → data-testid 属性

而且支持 **fallback 链**：主选择器失败时，自动依次尝试备用选择器。页面改版也不怕。

```json
{
  "selector": "#email",
  "selector_fallbacks": ["[name='email']", "[data-testid='email-input']"]
}
```

### 条件分支 — JSON 里的 if/else

不需要写 JavaScript，不需要写 Python。声明式条件直接在 JSON 里描述：

```json
{
  "action": "if",
  "condition": { "selector": ".status", "attribute": "textContent", "operator": "eq", "expected_value": "已认证" },
  "then": [ /* 条件满足时执行 */ ],
  "else": [ /* 条件不满足时执行 */ ]
}
```

支持两种条件模式：
- **基于状态**：元素是否存在、可见、选中
- **基于值**：元素属性值与期望值的比较（等于、不等、包含、正则匹配）

最多支持 3 层嵌套，足以应对绝大多数表单逻辑。

### 内置隐私脱敏

默认开启。自动检测并遮蔽：

- 邮箱地址 → `***@***.***`
- 电话号码
- 中国 18 位身份证号
- 美国 SSN

日志和提取输出自动处理，也可按字段粒度灵活控制。

### 失败诊断 — 为 AI Agent 设计的自我修复

当某个步骤失败时，引擎自动捕获：

1. **失败时刻的页面截图**
2. **页面上所有可见表单元素列表**（标签、name、ID、placeholder、data-testid 等）

这些诊断信息作为结构化的 JSON 返回，AI Agent 可以根据页面上**实际存在的元素**调整选择器策略并重试，无需从头开始。

### AI Agent 原生集成

这可能是 web-auto-form 最独特的特性。

项目内置了完整的 LLM function-calling 资源：
- **JSON Tool Schema** (`web_auto_form_tool.json`) — 可直接注册到任何兼容 OpenAI/Claude 的工具调用管道
- **System Prompt** (`SYSTEM_PROMPT.md`) — 触发条件、安全约束、输出格式
- **MCP Server** (`demo/mcp_server.py`) — 可直接接入 Claude Desktop 或 Cursor

**一句话就能让 AI 帮你填表**：对 Claude 说"用我的简历填一下这个职位申请表"，Claude 自动生成 JSON 配置并通过 web-auto-form 在真实浏览器中执行。

这形成了一种强大的分工模式：**LLM 负责规划和理解意图 → web-auto-form 负责确定性执行 → AI Agent 根据诊断结果自我修复**。

### 模板变量系统

```json
{
  "data": {
    "user": { "name": "张伟", "email": "zhangwei@example.com" },
    "position": "软件工程师"
  },
  "steps": [
    { "action": "fill", "selector": "#name", "value": "{{user.name}}" },
    { "action": "select", "selector": "#position", "value": "{{position}}" }
  ]
}
```

支持嵌套点号访问，CLI 可通过 `--data` 动态覆盖：

```bash
web_auto_form run config.json --data user.name=李四 --data position="产品经理"
```

---

## 与竞品的差异化定位

| 维度 | web-auto-form | Playwright | Selenium | Browser-Use |
|------|:---:|:---:|:---:|:---:|
| 工作方式 | JSON 配置 | 编写代码 | 编写代码 | 自然语言 |
| 学习曲线 | **零门槛** | 中等 | 陡峭 | 零门槛 |
| AI Agent 集成 | **原生支持** | 需自行封装 | 需自行封装 | 内置 |
| 执行确定性 | **100%** | 100% | 100% | 非确定（LLM 每步决策） |
| 执行成本 | 免费 | 免费 | 免费 | 每步 API 费用 |
| 隐私脱敏 | **内置** | 手动 | 手动 | 手动 |
| 最适合 | **表单填写** | Web 测试 | 老旧系统 | 一次性探索 |

**一句话选型：填表单 → web-auto-form；写测试 → Playwright；老旧系统 → Selenium；探索调研 → Browser-Use。**

---

## 快速上手

### 安装

```bash
pip install web-auto-form
playwright install chromium
```

### 第一个自动化

创建 `my_form.json`：

```json
{
  "url": "https://example.com/contact",
  "consent_statement": "自动填写联系表单用于测试。",
  "steps": [
    { "action": "fill", "selector": "#name", "value": "张三" },
    { "action": "fill", "selector": "#email", "value": "zhangsan@example.com" },
    { "action": "click", "selector": "button[type='submit']" }
  ]
}
```

```bash
web_auto_form run my_form.json
```

搞定。

### Python API 调用

```python
from web_auto_form import run

result = run({
    "url": "https://example.com/form",
    "consent_statement": "自动化测试。",
    "steps": [{"action": "fill", "selector": "#name", "value": "测试"}],
})

print(result["status"])     # "success"
print(result["extracted"])  # {"message": "提交成功"}
```

---

## 安全设计

作为一个浏览器自动化工具，安全是首要考虑：

- **consent 必填**：每次调用必须声明自动化目的，有头模式下展示并确认
- **浏览器沙箱**：默认开启，防止恶意页面逃逸
- **速率限制**：每步之间强制 500ms 延迟（可配置），防止触发反爬
- **PII 脱敏**：默认开启，日志和输出自动处理敏感信息
- **硬限制**：单次最多 50 步、嵌套最多 3 层、文件上传最多 50MB、总执行最多 5 分钟
- **明文禁止**：验证码绕过、凭证收割、无授权批量操作

---

## 项目状态

- **版本**：v0.1.0（Beta）
- **许可证**：MIT
- **Python**：3.9+
- **平台**：Windows / macOS / Linux
- **CI**：GitHub Actions，Python 3.9–3.12 全矩阵测试
- **代码质量**：flake8 + mypy + black + isort，完整类型注解

---

## 资源链接

- GitHub：[github.com/DUZ1287/WebAutoForm](https://github.com/DUZ1287/WebAutoForm)
- 在线 Playground：本地运行 `python playground/app.py`（Gradio 界面）
- 完整文档：`docs/` 目录下包含步骤参考、选择器指南、PII 脱敏详解、AI Agent 集成指南
- 示例配置：`examples/` 目录下包含完整职位申请、Google 表单、条件分支三个示例

---

## 结语

web-auto-form 解决的是一个非常具体但广泛存在的痛点：**我不想为填个表单写一堆代码**。

它的设计哲学很清楚——**做一件事，把它做好**。不是大而全的浏览器自动化框架，不是依赖 LLM 的非确定性代理。就是一个 JSON 进、结果出的表单填写引擎。

如果你经常和网页表单打交道，无论是手动批量录入数据，还是让 AI Agent 帮你自动操作，web-auto-form 都值得一试。

---

*web-auto-form — JSON in, forms out.*
